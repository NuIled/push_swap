#include "push_swap.h"


int main(int ac, char *av[])
{
    // int i  = 0;
    t_stack *stack;
    // int j = 0;
    stack = malloc(sizeof(t_stack));

    chek(av,ac,stack);

    // while (sizeof(stack->stacka[i]) == sizeof(int))
    //     {
    //         i++;
    //         j++;
    //     }
    // printf("\n%i\n",j);
}
